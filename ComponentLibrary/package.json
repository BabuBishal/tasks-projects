{
  "name": "bishalbabu-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -C apps/docs dev",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format"
  }
}
